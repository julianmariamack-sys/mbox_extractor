# 📬 MBOX Extractor

A lightweight tool to extract emails and attachments from `.mbox` files with optional GUI.

## Features
- Extract attachments
- Export emails as HTML
- Organize by Year / Month
- Optional grouping by sender
- GUI + CLI support

## CLI Usage

```bash
python extract.py mailbox.mbox --html-all -o output
